#include <stdio.h>
/**
 * main - causes an infinite loop
 * Return: 0
 */
int main(void)
{

	int i;
	printf("infinite loop incoming :(\n");
	
	/*
	 * 1 = 0'
	 * while (i < 10)
	 * {
	 * putchar(i);
	 * }
	 */

	printf("infinite loop avoide! \\o/\n");

	return (0);
}
